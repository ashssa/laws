diff --git a/dist/keystatic-core-api-generic.js b/dist/keystatic-core-api-generic.js
index aabdb1a49688d48a681ca4e06fb68ee7ca1e0869..98841eb0ac28484ecb639a70075ade596da28e7a 100644
--- a/dist/keystatic-core-api-generic.js
+++ b/dist/keystatic-core-api-generic.js
@@ -225,6 +225,8 @@ async function githubOauthCallback(req, config) {
     }
   });
   if (!tokenRes.ok) {
+    const errorText = await tokenRes.text();
+    console.error("GITHUB TOKEN EXCHANGE ERROR (!ok):", errorText);
     return {
       status: 401,
       body: 'Authorization failed'
@@ -235,20 +237,39 @@ async function githubOauthCallback(req, config) {
   try {
     tokenData = tokenDataResultType.create(_tokenData);
   } catch {
-    return {
-      status: 401,
-      body: 'Authorization failed'
-    };
+    console.error("GITHUB TOKEN EXCHANGE ERROR:", _tokenData);
+    if (_tokenData.access_token) {
+      tokenData = {
+        access_token: _tokenData.access_token,
+        expires_in: 31536000,
+        refresh_token: 'not-needed',
+        refresh_token_expires_in: 31536000,
+        scope: _tokenData.scope || ''
+      };
+    } else {
+      return {
+        status: 401,
+        body: 'Authorization failed'
+      };
+    }
   }
-  const headers = await getTokenCookies(tokenData, config);
-  if (state === 'close') {
+  try {
+    const headers = await getTokenCookies(tokenData, config);
+    if (state === 'close') {
+      return {
+        headers: [...headers, ['Content-Type', 'text/html']],
+        body: "<script>localStorage.setItem('ks-refetch-installations', 'true');window.close();</script>",
+        status: 200
+      };
+    }
+    return redirect(`/keystatic${from ? `/${from}` : ''}`, headers);
+  } catch (err) {
+    console.error("FATAL CALLBACK ERROR:", err);
     return {
-      headers: [...headers, ['Content-Type', 'text/html']],
-      body: "<script>localStorage.setItem('ks-refetch-installations', 'true');window.close();</script>",
-      status: 200
+      status: 500,
+      body: 'Callback crashed: ' + (err.message || String(err))
     };
   }
-  return redirect(`/keystatic${from ? `/${from}` : ''}`, headers);
 }
 async function getTokenCookies(tokenData, config) {
   const headers = [['Set-Cookie', cookie.serialize('keystatic-gh-access-token', tokenData.access_token, {
diff --git a/dist/keystatic-core-api-generic.node.js b/dist/keystatic-core-api-generic.node.js
index b4c82e2d82fcb7fb3fa05835047d92a88866a608..73fe3d2ff82bfe113ffb0510ea562fe984a7c3fb 100644
--- a/dist/keystatic-core-api-generic.node.js
+++ b/dist/keystatic-core-api-generic.node.js
@@ -428,6 +428,8 @@ async function githubOauthCallback(req, config) {
     }
   });
   if (!tokenRes.ok) {
+    const errorText = await tokenRes.text();
+    console.error("GITHUB TOKEN EXCHANGE ERROR (!ok):", errorText);
     return {
       status: 401,
       body: 'Authorization failed'
@@ -438,20 +440,39 @@ async function githubOauthCallback(req, config) {
   try {
     tokenData = tokenDataResultType.create(_tokenData);
   } catch {
-    return {
-      status: 401,
-      body: 'Authorization failed'
-    };
+    console.error("GITHUB TOKEN EXCHANGE ERROR:", _tokenData);
+    if (_tokenData.access_token) {
+      tokenData = {
+        access_token: _tokenData.access_token,
+        expires_in: 31536000,
+        refresh_token: 'not-needed',
+        refresh_token_expires_in: 31536000,
+        scope: _tokenData.scope || ''
+      };
+    } else {
+      return {
+        status: 401,
+        body: 'Authorization failed'
+      };
+    }
   }
-  const headers = await getTokenCookies(tokenData, config);
-  if (state === 'close') {
+  try {
+    const headers = await getTokenCookies(tokenData, config);
+    if (state === 'close') {
+      return {
+        headers: [...headers, ['Content-Type', 'text/html']],
+        body: "<script>localStorage.setItem('ks-refetch-installations', 'true');window.close();</script>",
+        status: 200
+      };
+    }
+    return redirect(`/keystatic${from ? `/${from}` : ''}`, headers);
+  } catch (err) {
+    console.error("FATAL CALLBACK ERROR:", err);
     return {
-      headers: [...headers, ['Content-Type', 'text/html']],
-      body: "<script>localStorage.setItem('ks-refetch-installations', 'true');window.close();</script>",
-      status: 200
+      status: 500,
+      body: 'Callback crashed: ' + (err.message || String(err))
     };
   }
-  return redirect(`/keystatic${from ? `/${from}` : ''}`, headers);
 }
 async function getTokenCookies(tokenData, config) {
   const headers = [['Set-Cookie', cookie.serialize('keystatic-gh-access-token', tokenData.access_token, {
